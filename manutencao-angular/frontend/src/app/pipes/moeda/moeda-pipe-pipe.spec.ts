import { MoedaPipePipe } from './moeda-pipe-pipe';

describe('MoedaPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MoedaPipePipe();
    expect(pipe).toBeTruthy();
  });
});
