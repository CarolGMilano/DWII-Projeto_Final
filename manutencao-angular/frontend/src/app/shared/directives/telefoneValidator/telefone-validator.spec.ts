import { TelefoneValidator } from './telefone-validator';

describe('TelefoneValidator', () => {
  it('should create an instance', () => {
    const directive = new TelefoneValidator();
    expect(directive).toBeTruthy();
  });
});
