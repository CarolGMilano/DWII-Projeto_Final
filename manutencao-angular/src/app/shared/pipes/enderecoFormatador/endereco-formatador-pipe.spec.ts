import { EnderecoFormatadorPipe } from './endereco-formatador-pipe';

describe('EnderecoFormatadorPipe', () => {
  it('create an instance', () => {
    const pipe = new EnderecoFormatadorPipe();
    expect(pipe).toBeTruthy();
  });
});
