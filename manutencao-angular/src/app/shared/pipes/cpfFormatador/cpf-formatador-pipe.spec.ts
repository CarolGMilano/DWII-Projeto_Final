import { CpfFormatadorPipe } from './cpf-formatador-pipe';

describe('CpfFormatadorPipe', () => {
  it('create an instance', () => {
    const pipe = new CpfFormatadorPipe();
    expect(pipe).toBeTruthy();
  });
});
